{
  "name": "single-spa",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run --parallel --stream start",
    "build": "lerna run --stream build"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "husky": "^8.0.2",
    "lerna": "^6.1.0"
  }
}
